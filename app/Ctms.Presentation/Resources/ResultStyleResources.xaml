<ResourceDictionary 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:waf="http://waf.codeplex.com/schemas"
    xmlns:s="http://schemas.microsoft.com/surface/2008"
    xmlns:c="clr-namespace:Ctms.Presentation.Converters">

    <!--
        This document is used for specific style definitions for the results view.
        Hints: 
            TargetType: Type of the elemnt that shall be affected.
            BasedOn: Optional attribute for deriving from other styles.
            Setters: Multiple elements that define style properties.
            StaticResource: Use of custom predefined common values like color.
    -->

    <!--Key styles-->
    <!--<Style x:Key="SearchOptionsTextBlock" TargetType="TextBlock">
        <Setter Property="Foreground" Value="#0fc" />
    </Style>-->
    <DataTemplate x:Key="ScatterViewItemDataTemplate">
            <Grid MinHeight="100" MinWidth="150" Margin="0,20,0,0" HorizontalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <TextBlock Margin="0,0,0,0" Background="#528B8B" TextAlignment="Center" Grid.Column="0" Grid.Row="0" Foreground="White" Text="{Binding Song.Title}"/>
                <TextBlock Padding="0,2,0,5" Margin="0" Background="#528B8B" TextAlignment="Center" Grid.Column="0" Grid.Row="1" Foreground="White" Text="{Binding Song.Artist}"/>
                <UniformGrid Grid.Row="2" Rows="1" Columns="3">
                    <Viewbox Stretch="Fill">
                        <Rectangle Fill="Blue" MinHeight="15" MinWidth="0.5"></Rectangle>
                    </Viewbox>
                    <Viewbox Stretch="UniformToFill">
                        <Rectangle Fill="Red" MinHeight="15" MinWidth="0.5"></Rectangle>
                    </Viewbox>
                    <Viewbox Stretch="UniformToFill">
                        <Rectangle Fill="Yellow" MinHeight="15" MinWidth="0.5" ></Rectangle>
                    </Viewbox>
                </UniformGrid>
                <TextBlock Padding="0,2,0,5" Margin="0,0,0,0" Background="#79CDCD" TextAlignment="Center" Grid.Column="0" Grid.Row="3" Foreground="White" RenderTransformOrigin="0.5, 0.5" Text="{Binding Song.Artist}">
                    <TextBlock.RenderTransform>
                        <RotateTransform Angle="180"/>
                    </TextBlock.RenderTransform>
                </TextBlock>
                <TextBlock Margin="0,0,0,0" Background="#79CDCD" TextAlignment="Center" RenderTransformOrigin="0.5, 0.5" Grid.Column="0" Grid.Row="4" Foreground="White" Text="{Binding Song.Title}">
                    <TextBlock.RenderTransform>
                        <RotateTransform Angle="180"/>
                    </TextBlock.RenderTransform>
                </TextBlock>
            </Grid>
    </DataTemplate>
    
    <Style x:Key="CursorStyle" TargetType="{x:Type ContentControl}">
        <Setter Property="Opacity" Value="0.50"/>
        <Setter Property="ContentTemplate" Value="{StaticResource ScatterViewItemDataTemplate}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ContentControl}">
                    <ContentPresenter 
            Content="{TemplateBinding Content}" 
            ContentTemplate="{TemplateBinding ContentTemplate}">
                    </ContentPresenter>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="Tag" Value="DragEnter">
                <Setter Property="Opacity" Value="1.0"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    
</ResourceDictionary>
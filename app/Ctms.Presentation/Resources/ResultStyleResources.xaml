<ResourceDictionary
    x:Class="Ctms.Presentation.Resources.ResultStyleResources"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:waf="http://waf.codeplex.com/schemas"
    xmlns:vm="clr-namespace:Ctms.Applications.ViewModels;assembly=Ctms.Applications"
    xmlns:s="http://schemas.microsoft.com/surface/2008"
    xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
    xmlns:c="clr-namespace:Ctms.Presentation.Converters">

    <!--
        This document is used for specific style definitions for the results view.
        Hints: 
            TargetType: Type of the elemnt that shall be affected.
            BasedOn: Optional attribute for deriving from other styles.
            Setters: Multiple elements that define style properties.
            StaticResource: Use of custom predefined common values like color.
    -->

    <!--Key styles-->
    <!--<Style x:Key="SearchOptionsTextBlock" TargetType="TextBlock">
        <Setter Property="Foreground" Value="#0fc" />
    </Style>-->
    <DataTemplate x:Key="ScatterViewItemDataTemplate">
        <ContentControl SizeChanged="ResultWrapper_SizeChanged" MinWidth="{Binding StdWidth}" MinHeight="{Binding StdHeight}">
            <Grid Name="ResultWrapper" Margin="0,0,0,0" Visibility="{Binding IsDetail, Converter={x:Static c:InverseBoolToVisibilityConverter.Default}}"
                  VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                <Grid Name="ResultGrid" Margin="0,0,0,0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock FontSize="14" Padding="10,2,10,2" Margin="0,0,0,0" Background="#222" TextAlignment="Center" Grid.Column="0" Grid.Row="0" Foreground="White" Text="{Binding Result.Song.Title}"/>
                    <TextBlock FontSize="12" Padding="10,2,10,2" Margin="0" Background="#222" TextAlignment="Center" Grid.Column="0" Grid.Row="1" Foreground="White" Text="{Binding Result.Song.ArtistName}"/>
                    <UniformGrid Grid.Row="2" Rows="1" Columns="3">
                        <Viewbox Stretch="Fill">
                            <Rectangle Fill="Blue" MinHeight="15" MinWidth="0.5"></Rectangle>
                        </Viewbox>
                        <Viewbox Stretch="UniformToFill">
                            <Rectangle Fill="Red" MinHeight="15" MinWidth="0.5"></Rectangle>
                        </Viewbox>
                        <Viewbox Stretch="UniformToFill">
                            <Rectangle Fill="Yellow" MinHeight="15" MinWidth="0.5" ></Rectangle>
                        </Viewbox>
                    </UniformGrid>
                    <Label Content="Loading" Visibility="{Binding IsLoading, Converter={x:Static c:BoolToVisibilityConverter.Default}}" Grid.Row="3" Grid.Column="0" />
                    <Label Content="Playing" Visibility="{Binding IsPlaying, Converter={x:Static c:BoolToVisibilityConverter.Default}}" Grid.Row="3" Grid.Column="1"/>
                    <TextBlock FontSize="12" Padding="10,2,10,2" Margin="0,0,0,0" Background="#222" TextAlignment="Center" Grid.Column="0" Grid.Row="3" Foreground="White" RenderTransformOrigin="0.5, 0.5" Text="{Binding Result.Song.ArtistName}">
                    <TextBlock.RenderTransform>
                        <RotateTransform Angle="180"/>
                    </TextBlock.RenderTransform>
                    </TextBlock>
                    <TextBlock FontSize="14" Padding="10,2,10,2" Margin="0,0,0,0" Background="#222" TextAlignment="Center" RenderTransformOrigin="0.5, 0.5" Grid.Column="0" Grid.Row="4" Foreground="White" Text="{Binding Result.Song.Title}">
                    <TextBlock.RenderTransform>
                        <RotateTransform Angle="180"/>
                    </TextBlock.RenderTransform>
                    </TextBlock>
                    <!--<Grid DataContext="{DynamicResource ViewModel}" Grid.Row="0" Grid.RowSpan="5" Grid.Column="0" Grid.ColumnSpan="2" Background="Transparent">
                <Grid.InputBindings>
                    <MouseBinding Gesture="LeftDoubleClick" Command="{Binding PrelistenCommand}" CommandParameter="{Binding Path=Content, RelativeSource={RelativeSource Mode=TemplatedParent}}" />
                </Grid.InputBindings>
            </Grid>-->
                </Grid>
                <Grid Name="DetailView" Visibility="{Binding IsDetail, Converter={x:Static c:BoolToVisibilityConverter.Default}}">
                    <Label Content="DetailView" FontSize="36" Foreground="Red"></Label>
                </Grid>
            </Grid>
        </ContentControl>
    </DataTemplate>
    
    <DataTemplate x:Key="TagInfluences">
        
    </DataTemplate>
    
    <Style x:Key="TextSize" TargetType="{x:Type TextBlock}">
        <Setter Property="FontSize" Value="20"/>
    </Style>

    <!-- Style for result items when dragged -->
    <Style x:Key="ResultCursorStyle" TargetType="{x:Type ContentControl}">
        <Setter Property="Opacity" Value="0.50"/>
        <Setter Property="ContentTemplate" Value="{StaticResource ScatterViewItemDataTemplate}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ContentControl}">
                    <ContentPresenter Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="Tag" Value="DragEnter">
                <Setter Property="Opacity" Value="1.0"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    
</ResourceDictionary>
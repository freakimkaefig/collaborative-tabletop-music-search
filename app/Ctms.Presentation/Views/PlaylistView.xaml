<UserControl x:Class="Ctms.Presentation.Views.PlaylistView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:p="clr-namespace:Ctms.Presentation.Properties"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:waf="http://waf.codeplex.com/schemas"
    xmlns:vm="clr-namespace:Ctms.Applications.ViewModels;assembly=Ctms.Applications"
    xmlns:s="http://schemas.microsoft.com/surface/2008"         
    IsEnabled="{Binding IsEnabled}"
    waf:ValidationHelper.IsValid="{Binding IsValid, Mode=OneWayToSource}">
    
    
    <!--mc:Ignorable="d" d:DataContext="{d:DesignData Source=../DesignData/SamplePersonViewModel.xaml}"
    waf:ValidationHelper.IsEnabled="true" -->
    <Grid Margin="0,0,0,0" Width="1920" Height="1080">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1920"/>
        </Grid.ColumnDefinitions>
        
        <s:SurfaceButton Content="Play" Width="100" Height="40" HorizontalAlignment="Left" Command="{Binding PlayCommand}" Margin="1700,996,0,44" />
        <s:SurfaceButton Content="Stop" Width="100" Height="40" HorizontalAlignment="Right" Command="{Binding StopCommand}" Margin="0,996,0,44" />
        <s:SurfaceListBox Name="PlaylistAddDropTarget" Height="400" AllowDrop="True" ItemsSource="{Binding ResultsForPlaylist}" ItemTemplate="{StaticResource SurfaceListBoxItemDataTemplate}"
            s:SurfaceDragDrop.DragEnter="PlaylistAddDropTarget_DragEnter" s:SurfaceDragDrop.DragLeave="PlaylistAddDropTarget_DragLeave" s:SurfaceDragDrop.Drop="PlaylistAddDropTarget_Drop" Margin="1700,590,0,90">
        </s:SurfaceListBox>
        <Label Content="PlaylistView" Height="38" Margin="1700,1042,0,0" />
    </Grid>
</UserControl>

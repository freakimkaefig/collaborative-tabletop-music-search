<s:TagVisualization x:Class="Ctms.Presentation.Views.SearchTagView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:s="http://schemas.microsoft.com/surface/2008"
    xmlns:p="clr-namespace:Ctms.Presentation.Properties"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:waf="http://waf.codeplex.com/schemas"
    xmlns:vm="clr-namespace:Ctms.Applications.ViewModels;assembly=Ctms.Applications"
    xmlns:ps="clr-namespace:PieInTheSky;assembly=PieInTheSky"
    IsEnabled="{Binding IsEnabled}"
    waf:ValidationHelper.IsValid="{Binding IsValid, Mode=OneWayToSource}"                    
    Loaded="SimpleVisualization_Loaded" >

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="C:/Git/Uni/CTMS/ASE/app/Ctms.Presentation/Resources/Images/Search/record.png" Opacity="1.0"/>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

            <!--Selection state-->
            <ps:PieMenu x:Name="Menu4"  Grid.Column="0" Grid.Row="0"
                   Canvas.Left="100"
                   Canvas.Top="100"
                   SelectedBackground="#7399"
                   Foreground="White"
                   BorderBrush="#000"
                   SectorGap="0"
                   Gap="0"
                   Radius="120"
                   InnerRadius="33"
                   RotateText="True"
                   RotateTextAngle="0"
                   Rotation="150"
                   MenuSector="240"
                    Background="Transparent">
                <ps:PieMenuItem Header="Audioslave" SubHeader="Show Me How To Live" BorderThickness="0" 
                                Command="{Binding SelectCommand}" Background="#0555">
                </ps:PieMenuItem>
            <ps:PieMenuItem Header="Madeon" SubHeader="Icarus" BorderThickness="0"  Background="#5555">
                </ps:PieMenuItem>
            <ps:PieMenuItem Header="Arvmin van Buuren" SubHeader="This is what it feels like" BorderThickness="0" Background="#0555">
                </ps:PieMenuItem>
            <ps:PieMenuItem Header="Spectrum" SubHeader="Zedd" BorderThickness="0"  Background="#5555">
                </ps:PieMenuItem>
            <ps:PieMenuItem  Header="Audioslave" SubHeader="Cochise" BorderThickness="0"  Background="#0555">
                </ps:PieMenuItem>
        </ps:PieMenu>
        <ps:PieMenu x:Name="Menu5"  Grid.Column="0" Grid.Row="0"
                   Canvas.Left="100"
                   Canvas.Top="100"
                   Background="Transparent"
                   SelectedBackground="#7399"
                   Foreground="White"
                   BorderBrush="#000"
                   SectorGap="0"
                   Gap="0"
                   Radius="120"
                   InnerRadius="33"
                   RotateText="True"
                   RotateTextAngle="-90"
                   Rotation="30"
                   MenuSector="120">
            <ps:PieMenuItem Header="Plan B" SubHeader="Stay Too Long wwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwww"  
                            BorderThickness="0" FontSize="16"
                    CenterText="true" Command="{Binding SelectCommand}" Background="#5555">
            </ps:PieMenuItem>
        </ps:PieMenu>
    </Grid>
</s:TagVisualization>
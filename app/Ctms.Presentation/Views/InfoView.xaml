<UserControl x:Class="Ctms.Presentation.Views.InfoView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:p="clr-namespace:Ctms.Presentation.Properties"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:waf="http://waf.codeplex.com/schemas"
    xmlns:vm="clr-namespace:Ctms.Applications.ViewModels;assembly=Ctms.Applications"
    xmlns:dm="clr-namespace:Ctms.Applications.DataModels;assembly=Ctms.Applications"
    xmlns:s="http://schemas.microsoft.com/surface/2008"
    IsEnabled="{Binding IsEnabled}"
    waf:ValidationHelper.IsValid="{Binding IsValid, Mode=OneWayToSource}">

    <UserControl.Resources>
        <!--<DataTemplate DataType="{x:Type dm:InfoDataModel}">
            <Grid>
            </Grid>
        </DataTemplate>-->
    </UserControl.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
        </Grid.RowDefinitions>
        <!--<Label Content="HAAAAAALLLOOOO" Width="200" Height="100" Foreground="White" Background="#333" HorizontalAlignment="Center" VerticalAlignment="Center"/>-->
        <ListBox ItemsSource="{Binding CommonInfos}" BorderBrush="Transparent" Background="Transparent"
                 HorizontalContentAlignment="Stretch">

            <!--Grid.IsSharedSizeScope="True"-->
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="4" ><!--Style="{StaticResource CommonInfo}"-->
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Grid.ColumnSpan="3" Text="{Binding Info.MainText}" FontWeight="Bold"/>
                        <TextBlock Grid.Row="1" Grid.ColumnSpan="3" Text="{Binding Info.Subtext}" FontWeight="Bold"/>
                        <s:SurfaceButton Grid.Row="1" Grid.ColumnSpan="3" Content="Ok" />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!--<TextBlock Text="Info" />-->
    </Grid>

</UserControl>

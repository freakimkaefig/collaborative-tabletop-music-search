<Window x:Class="Ctms.Presentation.Views.ShellWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    xmlns:waf="http://waf.codeplex.com/schemas"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:s="http://schemas.microsoft.com/surface/2008"
    mc:Ignorable="d"     
    MinWidth="1280"  MinHeight="700" 
        >
<!--d:DataContext="{d:DesignInstance dd:SampleShellViewModel, IsDesignTimeCreatable=True}"
    Title="{Binding Title}" Icon="{StaticResource BookLibraryImageSource}"
    waf:ValidationHelper.IsEnabled="true" waf:ValidationHelper.IsValid="{Binding IsValid, Mode=OneWayToSource}"-->

    <!--xmlns:p="clr-namespace:Ctms.Presentation.Properties"
    
    xmlns:dd="clr-namespace:Ctms.Presentation.DesignData"
    xmlns:c="clr-namespace:Ctms.Presentation.Converters"-->

    <Window.Background>
        <RadialGradientBrush
            GradientOrigin="0.5,0.5"
            Center="0.5,0.5" 
            RadiusX="0.5" RadiusY="0.5">
            <RadialGradientBrush.GradientStops>
                <GradientStop Color="#046" Offset="0" />
                <GradientStop Color="#003" Offset="1.0" />
            </RadialGradientBrush.GradientStops>
        </RadialGradientBrush>
    </Window.Background>

    <Window.Resources>
        <Style x:Key="CommonStyle" TargetType="FrameworkElement">
            <Setter Property="Margin" Value="2" />
        </Style>
        <Style TargetType="StackPanel" BasedOn="{StaticResource CommonStyle}">
        </Style>
        <Style TargetType="Grid" BasedOn="{StaticResource CommonStyle}">
        </Style>
        <Style TargetType="Button" BasedOn="{StaticResource CommonStyle}">
            <Setter Property="Foreground" Value="#fff" />
        </Style>
        <Style TargetType="Label" BasedOn="{StaticResource CommonStyle}">
            <Setter Property="Foreground" Value="#fff" />
        </Style>
        <Style TargetType="TextBlock" BasedOn="{StaticResource CommonStyle}">
            <Setter Property="Foreground" Value="#fff" />
        </Style>
        <Style TargetType="TextBox" BasedOn="{StaticResource CommonStyle}">
            <Setter Property="Foreground" Value="#fff" />
        </Style>
    </Window.Resources>
    <s:TagVisualizer x:Name="TagVisualizer" Width="Auto" Height="Auto" >

        <Grid Margin="0,0,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>


            <ContentControl Content="{Binding ShellService.SearchView}" Margin="0,0,0,0" Grid.Column="0" Grid.Row="0"/>
            <ContentControl Content="{Binding ShellService.ResultView}" Margin="0,0,0,0" Grid.Column="1" Grid.Row="0"/>
            <ContentControl Content="{Binding ShellService.DetailView}" Margin="0,0,0,0" Grid.Column="2" Grid.Row="0"/>
            <ContentControl Content="{Binding ShellService.MenuView}" Margin="0,0,0,0" Grid.Column="3" Grid.Row="0"/>
            <!--<ContentControl Content="{Binding ShellService.PlaylistView}" Grid.Row="0" Grid.Column="0" Margin="0,0,0,0"/>-->
        </Grid>
    </s:TagVisualizer>
</Window>

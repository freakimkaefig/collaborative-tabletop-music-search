<UserControl x:Class="Ctms.Presentation.Views.ResultView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:p="clr-namespace:Ctms.Presentation.Properties"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:waf="http://waf.codeplex.com/schemas"
    xmlns:vm="clr-namespace:Ctms.Applications.ViewModels;assembly=Ctms.Applications"
    MinWidth="250" MinHeight="400" IsEnabled="{Binding IsEnabled}"
    waf:ValidationHelper.IsEnabled="true" waf:ValidationHelper.IsValid="{Binding IsValid, Mode=OneWayToSource}">
    <!--mc:Ignorable="d" d:DataContext="{d:DesignData Source=../DesignData/SamplePersonViewModel.xaml}"-->
    <Grid Margin="0,0,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Label Content="ResultView" Grid.Column="0" Grid.Row="0"/>

        <!--<Label Content="Json Response (for development)" Target="{Binding ElementName=jsonResponseBox}" Grid.Column="0" Grid.Row="1"/>
        <TextBox x:Name="jsonResponseBox" Text="{Binding Search.JsonQuery, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=true, 
                     ValidatesOnDataErrors=true, NotifyOnValidationError=true}" 
                 MaxLength="30" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" HorizontalAlignment="Stretch"/>-->

        <!--<TextBox x:Name="songTitle" Text="{Binding Result.Song.Title, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=true, 
                 ValidatesOnDataErrors=true, NotifyOnValidationError=true}" 
                 Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" HorizontalAlignment="Stretch"/>-->
        <Label Content="{Binding Result.Song.Title}" Grid.Column="1" Grid.Row="1"/>
    </Grid>
</UserControl>

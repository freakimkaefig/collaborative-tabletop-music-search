<UserControl x:Class="Ctms.Presentation.Views.SearchView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:p="clr-namespace:Ctms.Presentation.Properties"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:waf="http://waf.codeplex.com/schemas"
    xmlns:vm="clr-namespace:Ctms.Applications.ViewModels;assembly=Ctms.Applications"
    xmlns:ps="clr-namespace:PieInTheSky;assembly=PieInTheSky"
    xmlns:s="http://schemas.microsoft.com/surface/2008"
    MinWidth="450" MinHeight="550" IsEnabled="{Binding IsEnabled}"
    waf:ValidationHelper.IsValid="{Binding IsValid, Mode=OneWayToSource}">
    
    <!--mc:Ignorable="d" d:DataContext="{d:DesignData Source=../DesignData/SamplePersonViewModel.xaml}"
    waf:ValidationHelper.IsEnabled="true" -->


    <s:TagVisualizer x:Name="SearchTagVisualizer" Width="Auto" Height="Auto" VisualizationAdded="OnVisualizationAdded"                      
                     VisualizationRemoved="OnVisualizationRemoved">

        <Grid Margin="0,0,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <Label Content="SearchView" Grid.Column="0" Grid.Row="0"/>
            <Button Content="Search/Refresh" Command="{Binding StartSearchCmd}" Grid.Column="0" Grid.Row="1" Width="100" Height="50" HorizontalAlignment="Left"/>
            <TextBox Grid.Column="0" Grid.Row="2" Text="{Binding InputValue}" Width="140"/>
            <TextBlock Grid.Column="0" Grid.Row="3"  HorizontalAlignment="Left" Style="{StaticResource SearchOptionsTextBlock}"
                Text="ControlResources Key Example"/>

            <Grid Grid.Row="4">
                <Grid.Background>
                    <ImageBrush ImageSource="C:/Git/Uni/CTMS/ASE/app/Ctms.Presentation/Resources/Images/Search/record.png" Opacity="1.0"/>
                </Grid.Background>
                <Grid.RowDefinitions>
                    <RowDefinition Height="2*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <!--Selection state-->
                <ps:PieMenu x:Name="Menu4"  Grid.Column="0" Grid.Row="0"
                       Canvas.Left="100"
                       Canvas.Top="100"
                       SelectedBackground="#7399"
                       Foreground="White"
                       BorderBrush="#000"
                       SectorGap="0"
                       Gap="0"
                       Radius="120"
                       InnerRadius="33"
                       RotateText="True"
                       RotateTextAngle="0"
                       Rotation="150"
                       MenuSector="240"
                        Background="Transparent">
                    <ps:PieMenuItem Header="Audioslave" SubHeader="Cochise" BorderThickness="0" 
                                    Command="{Binding SelectOptionCommand}" Background="#0555">
                    </ps:PieMenuItem>
                    <ps:PieMenuItem Header="Katy Perry" SubHeader="Cochise" BorderThickness="0"  Background="#5555">
                    </ps:PieMenuItem>
                    <ps:PieMenuItem Header="Arvmin van Buuren" SubHeader="Cochise" BorderThickness="0" Background="#0555">
                    </ps:PieMenuItem>
                    <ps:PieMenuItem Header="Zedd" SubHeader="Cochise" BorderThickness="0"  Background="#5555">
                    </ps:PieMenuItem>
                    <ps:PieMenuItem  Header="Pink" SubHeader="Cochise" BorderThickness="0"  Background="#0555">
                    </ps:PieMenuItem>
                </ps:PieMenu>
                <ps:PieMenu x:Name="Menu5"  Grid.Column="0" Grid.Row="0"
                       Canvas.Left="100"
                       Canvas.Top="100"
                       Background="Transparent"
                       SelectedBackground="#7399"
                       Foreground="White"
                       BorderBrush="#000"
                       SectorGap="0"
                       Gap="0"
                       Radius="120"
                       InnerRadius="33"
                       RotateText="True"
                       RotateTextAngle="-90"
                       Rotation="30"
                       MenuSector="120">
                    <ps:PieMenuItem Header="Elvis" SubHeader="Cochise"  
                                BorderThickness="0" FontSize="16"
                                Id="{Binding MainItemId}"
                                CenterText="true" Command="{Binding SelectOptionCmd}" Background="#5555">
                    </ps:PieMenuItem>
                </ps:PieMenu>
            </Grid>

        </Grid>
        
    </s:TagVisualizer>
    
</UserControl>
